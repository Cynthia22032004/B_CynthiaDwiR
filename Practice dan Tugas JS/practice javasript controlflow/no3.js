const people = ["Scooby", "Velma", "Daphne", "Shaggy", "Fred"];

// Using a for loop to iterate over the array and print each name in uppercase
for (let i = 0; i < people.length; i++) {
    console.log(people[i].toUpperCase());
}
